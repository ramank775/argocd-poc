project:
  name: dev-apps
  cluster: dev-cluster
  namespace: argocd
  env: dev

repo:
  url: https://github.com/ramank775/iac-template.git
  branch: dev

apps:
  - name: web
    enabled: true
    chart: web
    config: web.yaml
  - name: backend
    enabled: true
    chart: backend
    config: backend.yaml
